# brawlfarm board

Living board. Finished work moves to the top with its proof.

## Done

- Phase 11 (in progress): the panel critique. A harsh design critique of the React panel was published as a review artifact on 2026-09-17 (86 items over 12 screens, real captures against wireframes, six personas, eight questions); the owner answered it and gave go, and the decisions are in `docs/superpowers/specs/2026-09-17-panel-critique.md`. Eight pull requests follow in order, each sonnet-reviewed per task and as a whole branch, nothing under `brawlfarm/core`. Copy and glossary, PR https://github.com/as9pa/brawlfarm/pull/23 (merged 2026-09-17, CI green: 883 pytest, 562 vitest, ruff and scrub clean): `lib/copy.ts` is the one place shared nouns are written; the Fleet alert strip says one sentence per alert kind and never prints raw fields; feed lines are sentences with a fallback that prints only human fields; none and True are never rendered and counts go through Intl; the schedule override chip reads Paused until or Running until; a stopped card says Start; every HTTP error maps to a sentence with a next step; worker, supervisor and tick appear in no string a person reads, enforced by `lib/words.guard.test.ts`. Next: the component kit, fleet and shell, instance page, stats, settings and setup, calibration, narrow screens, then version 1.1.0.
- Phase 10 (in progress): full-size observe frames, roster recalibration, quest-aware pick and auto-upgrade. Gap fix, PR https://github.com/as9pa/brawlfarm/pull/19 (merged 2026-09-16, sonnet-reviewed, CI green: 783 pytest, 497 vitest, ruff and scrub clean): `preview.encode` takes `full=True`, `Recorder(full_size=True)` skips the resize, and observe mode records at 1600x900 so template crops and tap coordinates come straight off the frames; farm-mode recordings stay 800x450, and the 512 MiB cap now lands before the 2000 frame cap. The owner's first observe session (100 frames at 800x450, 2026-09-16) showed the phase 9 spec wrong on the quests screen (a 3-row grid scrolling left to right, two-line card titles, up to three brawler names per quest, a REROLL QUEST button top right that joins never-tap) and on the roster (column-major, 3 rows, horizontal scroll; the header region, sort label and both toggles miss on the current UI, the sort tap lands on the quest toggle). Session 2 recorded at full size on 2026-09-16 (181 frames, 1600x900). Roster calibration, PR https://github.com/as9pa/brawlfarm/pull/20 (merged 2026-09-17, owner-approved, three task reviews and a whole-branch review on sonnet, CI green: 796 pytest, 497 vitest, ruff and scrub clean): every roster constant re-measured on the recording (rows 11 to 13 px lower than the doubled estimate, column pitch 380, offer panel never-tap at x 0 to 125, sort dropdown region relocated), `_visible_cards` snaps a name label to its card and ignores the half-cut fourth column, `_scroll_grid` takes a direction, and `select_brawler_by_name_checked` scans by name with no index arithmetic; live pass on Pie64 selected AMBER (0 swipes), ZIGGY (12) and COLT (2) with detail verification and zero never-tap refusals. Quest-aware pick, PR https://github.com/as9pa/brawlfarm/pull/21 (merged 2026-09-17, owner-approved calibration PR, five task reviews and a whole-branch review on sonnet, CI green: 870 pytest, 508 vitest, ruff and scrub clean): `questpick` parses quests naming several brawlers and the counts, glued words and glued OR the live OCR produces; `quests.visit` sweeps the quest grid by swipes only and reads the cards; the `quest_aware` plan flag (off by default, ladder only) runs one quests visit before the session-start select and picks an owned brawler under the goal that clears a quest, the plan target first; one `quest_pick` feed row per session; the "Pick quest brawlers" switch in the Farm plan card; REROLL QUEST and the roster offer panel join never-tap with a source-enforced rail; the QUEST_* block re-measured on session 2. Live pass on Pie64: with the switch on the worker read 51 cards and chose NORI for a quest naming NORI; with it off, no quests visit. Mode navigation and DND timing, PR https://github.com/as9pa/brawlfarm/pull/22 (merged 2026-09-17, owner-approved calibration PR, two task reviews and a whole-branch review on sonnet, CI green: 883 pytest, 508 vitest, ruff and scrub clean): the event picker gained tabs and one-time "NEW!" card covers, so `navigate_to_trio_showdown` taps the TROPHIES tab and peels the covers by OCR before opening SHOWDOWN and TRIO; the DND panels get 8 s and 6 s to open and the exit loop never taps the hamburger. Live pass on Pie64 starting from SOLO SHOWDOWN: the DND step finished in 20 s, the picker went TROPHIES tab, SHOWDOWN card, TRIO with the template verifying at 0.998, and the worker queued and played one game. Next: the auto-upgrade PR, owner-approved with every changed value listed, and the CONTRIBUTING never-tap amendment only after an explicit owner go.
- Phase 9: observe-only recording mode plus the quest and upgrade decision modules. PR https://github.com/as9pa/brawlfarm/pull/18 (merged 2026-09-13, sonnet-reviewed per task and whole branch, CI green: 780 pytest, 497 vitest, ruff and scrub clean). New: `brawlfarm/core/observer.py` (screencap, classify, record; imports nothing that can tap, pinned by `tests/test_observer.py`), `python -m brawlfarm.worker --observe`, the supervisor's third desired mode `observe` (precedence run, observe, stop; a live worker of the wrong kind is stopped and relaunched), `POST /api/instances/{name}/observe`, recorder payloads carrying `mode`, the Calibration page's "Record while I play" switch, and the plain recorder switch deferring to observe mode (route 409, switch disabled). Pure modules with no wiring yet: `brawlfarm/core/questpick.py` (parse quests, resolve the brawler to pick) and `brawlfarm/core/upgrade_gate.py` (`should_upgrade` with the coin floor). Live pass on Pie64: observe on launched the worker with `--observe`, 43 labeled frames landed under `calibration/recordings/Pie64/`, no tap, swipe or key event in any log; observe off dropped `record.flag`, the worker exited and the stop park was back. Next: the owner records two sessions (quests screen scrolled top to bottom with a win-N-with-brawler quest; brawler detail screen with Upgrade live, short on power points, short on coins, max level); wiring questpick and upgrade_gate into the farm loop, the `config.py` values and the CONTRIBUTING never-tap amendment come in owner-approved calibration PRs.
- Housekeeping after phase 8 (owner said all on 2026-09-13): PyPI release workflow, PR https://github.com/as9pa/brawlfarm/pull/15 (merged 2026-09-13, sonnet-reviewed, CI green): `.github/workflows/release.yml` builds the wheel on a published GitHub release (panel built first, tag must equal the package version, wheel content asserted) and publishes through PyPI trusted publishing from the `pypi` environment; `pyproject.toml` gained project URLs, classifiers, keywords and a `desktop` extra mirroring the dependency group; `docs/release.md`, README and `docs/setup.md` "From PyPI" sections. The one-time owner steps (a pending publisher on PyPI for owner as9pa, repo brawlfarm, workflow release.yml, environment pypi; the `pypi` GitHub environment; a release tagged like v1.0.0) are still open. CI flake fix, PR https://github.com/as9pa/brawlfarm/pull/16 (merged 2026-09-13): the last-session cache in `brawlfarm/api/sessions.py` keyed on mtime alone and missed two writes inside one filesystem tick on the windows runner; the key is now (mtime, size) with a deterministic test that freezes mtime. README screenshots, PR https://github.com/as9pa/brawlfarm/pull/17 (merged 2026-09-13, sonnet-reviewed, CI green): four 1440x900 captures of Fleet, Instance, Settings and Calibration taken from a scratch data home with the fixture tag only, plus the Fleet preview overlay fix the captures exposed (the screencap error and the "Break until" caption were two overlays drawn on top of each other; now one stacked column, with a vitest).
- Phase 8: After v1 (calibration page, queue recalibration, desktop window, frame recorder). PR https://github.com/as9pa/brawlfarm/pull/14 (merged 2026-09-12). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34718581877 green on the PR head (490 vitest tests in 67 files, 723 pytest, ruff clean, scrub check 0 hits); eight plan tasks plus one fix commit, shipped on typecheck, tests, scrub and the live pass under the opt-in review policy. Owner-approved calibration changes: `matchmaking.png` re-shot from the current "Players found N/12" banner (276x45, gray), `MATCHMAKING_THRESHOLD` 0.90 -> 0.85, `COLOR_ONLY_TEMPLATES` drops "matchmaking"; corpus check 71 of 71 queue frames matched with 0 false positives and a highest off-queue score of 0.5043. New: `<home>/calibration/calibration.toml` and `templates/` overrides read by `brawlfarm/core/calibration.py`, `vision.threshold_for` as the single threshold source, `GET /api/calibration` and the per-instance scores and recorder routes, the Calibration page at `/calibration` (frame overlay with anchor boxes, anchor table, overrides table, open-folder button, recorder switch), `brawlfarm/core/recorder.py` writing labeled JPEG sessions with a `labels.jsonl` sidecar under `<home>/calibration/recordings/` (caps 2000 frames or 512 MiB), `brawlfarm --window` with pywebview and a pystray tray icon behind the opt-in `desktop` dependency group, `docs/calibration.md`. Live pass on one instance with the recorder on: two full games, queue detected both times, no queue_timeout, 92 labeled frames (16 matchmaking), recorder switched off cleanly through the API; the pass exposed that the scores route scored the half-size preview at native size, fixed on the branch by scaling to 1600x900 first. Desktop pass: window opened, API answered, closing the window hid it with the process alive, kill by PID left no orphan; tray clicks and the hide balloon are owner checks. Deferred: brawler screen and event anchors wait for a recorder session with the owner playing; README screenshots; quest-aware brawler choice and opt-in auto-upgrade stay on hold by owner decision.
- Phase 7: docs and publish. PR https://github.com/as9pa/brawlfarm/pull/13 (merged 2026-09-12). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34686939050 green on the PR head (467 vitest tests in 64 files, 690 pytest, ruff clean, scrub check 0 hits); README rewritten for v1.0.0 with a Contributing section, new `docs/setup.md` (BlueStacks, Android Debug Bridge, display, install, the wizard step by step, the API token, where files live, first session, troubleshooting) and new `CONTRIBUTING.md` (the safety rails as a review blocker and the checks to run); every README command re-run in a fresh clone and `brawlfarm 1.0.0` reported; version 1.0.0 in the package, the web app and the lock file. Before publishing, the full history (165 commits, every branch) was audited commit by commit with the scrub hashes and rewritten with `git filter-repo`: the two blobs of the port commit that still carried legacy identifiers now read REDACTED, every author and committer is the GitHub noreply identity, the tree of main is byte-identical before and after, and the audit over the rewritten history reports 0 hits. Then tag v1.0.0, release https://github.com/as9pa/brawlfarm/releases/tag/v1.0.0, the repository made public on 2026-09-12 with topics brawl-stars, bluestacks, automation, opencv, fastapi, react and windows. Deferred: README screenshots wait for the owner's own captures; there is no PyPI package, so install is clone plus `uv sync`; the recalibration walk-through still waits for the owner to drive the game.
- Phase 6: Stats with brawler icons. PR https://github.com/as9pa/brawlfarm/pull/12 (merged 2026-09-12). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34682040350 green on the pushed head (467 vitest tests in 64 files, 690 pytest, ruff clean, scrub check 0 hits); ten plan tasks shipped on typecheck, tests, scrub and the live pass with no review dispatches (the opt-in review policy), plus one fix wave of six commits for the live pass findings; live run against the real install on a scratch data home seeded with a copy of the owner's game history: the Stats page drew the week's 37 games as a cumulative line, the crosshair walked every point by keyboard with a live-region readout, the Table view and Export CSV followed the selected range, the brawler table sorted by Net both ways with aria-sort following, the four connection states (no token, rejected, no tag, ok) each rendered the brief's sentence on Stats and on the Instance roster and flipped at once on a credential change, the icon route answered 200 with a week-long cache header, an ETag and a 304, the prewarm filled the roster's portraits within ten seconds of a restart, a stopped instance's page showed "Session ended 22:00" with its figures on a cold load, and a stale link naming a removed instance dropped it without a 404; keyboard, phone-width and light-theme passes done, with the six findings they raised fixed and three re-verified live before the PR opened (a failed catalog refresh no longer arms the hour throttle, times carry the day off Today, the Table toggle reports aria-pressed, the metrics row wraps at 390, Home and End move every segmented group, y-axis labels are thinned; blurred screenshots kept out of the repo). Not exercised: the unreachable connection state (needs the network pulled). Deferred: last_session counts games.csv rows inside the session window, so a session whose games the API never logged shows 0 games; an unmatched /api path with an encoded slash falls through to the SPA as 200 text/html (pre-existing); the Stats page shows the word Stats twice (TopBar h1 and page h2), like the Instance page; the first cell of the wrapped metrics row keeps its divider border at phone width (cosmetic); the supervisor still reports scheduled_break for one tick after Add instance; an instance folder orphaned by Remove still cannot be deleted from the Data section.
- Phase 5: Setup wizard and Settings screens. PR https://github.com/as9pa/brawlfarm/pull/9 (merged 2026-09-12). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34667974577 green on the pushed head (381 vitest tests in 55 files, 617 pytest, ruff clean, scrub check 0 hits); eleven plan tasks, the first eight reviewed on sonnet with fix rounds closed in one round each, the last three shipped on typecheck, tests, scrub and the live pass after the review policy became opt-in per project; live run against the real BlueStacks install on a scratch data home: the wizard found adb, listed the three instances with their ports, probed a stopped port and said so, measured the display, took a player tag typed lower-case and gave it back upper-cased with its hash, and the Done step started the instance and landed on Fleet with its card walking to Farming in about a minute; Settings removed a stopped instance, refused a farming one with the API's own 409 inline, answered a notification test for a throwaway ntfy topic, opened the data folder in Explorer, and reset every section while keeping the instance and its folder; keyboard, reduced-motion, phone-width and light-theme passes done, with the two phone-width failures they found fixed on the branch (blurred screenshots kept out of the repo). Deferred: the Stats page's prompts for a missing player tag and for an API key that stopped working are phase 6; an instance folder orphaned by Remove cannot be deleted from the Data section; the supervisor reports scheduled_break for one tick after Add instance; `docs/setup.md` with pictures is phase 7; the wizard's Display step is re-run on every visit because nothing about it is stored.
- Phase 4: UI shell, Fleet and Instance screens. PR https://github.com/as9pa/brawlfarm/pull/5 (merged 2026-09-11). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34596842452 green on the pushed head (264 vitest tests in 33 files, 575 pytest, ruff clean, scrub check 0 hits, wheel carries web/dist and no sources); twelve plan tasks each reviewed with fix rounds closed by scoped re-reviews, a three-slice final whole-branch review and one fix wave of fifteen commits re-reviewed clean; live run on the real instance driven from the panel: start walked the card's chip from Starting to Farming, the feed streamed a session of three games without a reload, a plan edit reached farmplan.json and read back, Stop showed "Stopping after this match" then Stopped, and killing the server by PID turned the connection pill Reconnecting in 172 ms and Live 3.3 s after it came back (scrubbed transcript and timings in the PR; blurred screenshots kept out of the repo). Deferred: which of a duplicated feed moment to show (domain event or the worker's step mirror) is an owner copy decision; the alerts store does not de-duplicate rows; the Fleet empty state and the Stats and Settings placeholders render a second h1 until phases 5 and 6 replace them; the disabled Start reason does not mention the 0.5 h minimum; a stopped instance's Session panel shows zeros on a cold load until an API-side last-session block exists (phase 6 candidate); the live run showed recurring queue_timeout recoveries after the game update, so a read-only calibration audit ran (menu and in-match anchors still match) and template recutting is its own reviewed PR with the owner driving the game.
- Phase 3: API and events. PR https://github.com/as9pa/brawlfarm/pull/3 (merged 2026-09-10). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34539932576 green on the merged head (540 tests, ruff clean, scrub check 0 hits); final whole-branch review MERGE with no blockers; live run on one BlueStacks instance driven entirely through the API: setup scan, display check and a 1600 x 900 screenshot answered, a start request took the instance from stopped through starting and farming to stopping and stopped over the event stream, the feed streamed the match, games.csv grew by 26 rows and the stats route reported the day (scrubbed excerpts in the PR). Deferred: the manual controls do not take the tick lock (consistency only; only the tick launches, so one worker per instance holds); trophies per hour needs a floor and 2-dp hours in the Stats UI (phase 6); the roster and queue for the plan editor join GET /plan in phase 4; the setup wizard must persist the discovered adb path into connection.adb_path (phase 5); core/events.py refresh prints still reach stdout; alerts are in memory only and vanish on restart.
- Phase 2: settings and supervisor. PR https://github.com/as9pa/brawlfarm/pull/2 (merged 2026-09-10). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34473328978 green on the merged head (416 tests, ruff clean, scrub check 0 hits); live run on one BlueStacks instance: the supervisor launched the worker with the settings env, showed starting, farming, stopping and stopped in turn, the worker played 2 matches and honoured the graceful stop at the menu without the 110 s escalation (excerpts in the PR). Deferred: the healthchecks ping (phase 3); a conftest reset of the notify and scheduler globals and the --no-launch help text (phase 3); schedule off means always-run, so Stopped only comes from the user's stop (owner to confirm the wording); battle-log stats stayed empty because the owner's API token is IP-restricted.
- Phase 4 follow-ups (owner decisions from the review packet, 2026-09-11): the feed hides a worker step mirror that repeats a domain event's sentence within 2 s, PR https://github.com/as9pa/brawlfarm/pull/7 (merged 2026-09-11, reviewed, CI green, 272 vitest); README screenshots wait for the owner's own captures; the recalibration of the core to the updated game UI is accepted as its own reviewed PR with the owner driving the game (first finding: the matchmaking screen changed to "Players found N/12" and the old anchor never matches, which is the queue-timeout loop); an API-side last-session block for cold loads joins the phase 6 queue.
- Live preview: the Instance page and Fleet cards show the worker's own frame (a 1 s JPEG written next to status.json, served by GET /api/instances/{name}/preview.jpg with a change check and a throttled live fallback for stopped instances; Instance page polls every 1 s, Fleet cards every 5 s, only while visible), PR https://github.com/as9pa/brawlfarm/pull/8 (merged 2026-09-11, sonnet review approved, CI run https://github.com/as9pa/brawlfarm/actions/runs/34650689610 green, 597 pytest, 280 vitest, scrub check 0 hits, live capture check against the real instance).
- Housekeeping after phase 5 (owner approved 2026-09-12): the toast bar drains with one CSS transition instead of a 100 ms timer, PR https://github.com/as9pa/brawlfarm/pull/10 (merged 2026-09-12, CI run https://github.com/as9pa/brawlfarm/actions/runs/34672986687 green, 381 vitest); event captures are capped at the newest 100 frames with one frame per reason per 10 minutes in the new core/captures.py, the controller delegating in one line, PR https://github.com/as9pa/brawlfarm/pull/11 (merged 2026-09-12, CI run https://github.com/as9pa/brawlfarm/actions/runs/34673120916 green, 625 pytest, scrub check 0 hits). The recalibration walk-through still waits for the owner to drive the game.
- Housekeeping: PR https://github.com/as9pa/brawlfarm/pull/6 (merged 2026-09-11) fixed the flaky tests/test_supervisor_process.py::test_pid_alive on the windows runner (it failed on main twice and on PR #5's first attempt): after a kill, the PID lingers in the kernel's process list for a moment and psutil reports it running, so pid_alive now confirms with OpenProcess plus WaitForSingleObject and can only move an answer from alive to dead for a process that has provably exited (the one-worker-per-instance direction is unchanged); proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34599140455 green, the file looped 40 times locally without a failure. Also gitignores .claude/worktrees/.
- Housekeeping: PR https://github.com/as9pa/brawlfarm/pull/4 (merged 2026-09-10) retired every comment and docstring citation of legacy notes that were never ported (comments and docstrings only, AST-equal on all 20 files, 540 tests, scrub check 0 hits), which closes the phase 1 deferred item about legacy doc paths, and added docs/notes/2026-09-10-frame-capture-assessment.md: direct window capture of BlueStacks is not worth it (adb capture measured at 172 ms, about a third of the loop, and window frames would need rescaling that breaks the template thresholds), so the frame-timing instrumentation was not added.
- Phase 1: repo and core. PR https://github.com/as9pa/brawlfarm/pull/1 (merged 2026-09-10). Proof: CI run https://github.com/as9pa/brawlfarm/actions/runs/34446450332 green on the merged head (336 tests, ruff clean, scrub check 0 hits); farm core ported from the legacy bot package with identity removed; final whole-branch review MERGE with no blockers. Deferred: conftest isolation does not reach module-level cached paths in core/config, core/datalog and core/stats (fix in phase 2 with the settings model); ported test docstrings still cite legacy doc paths; CI actions warn about Node 20 deprecation.

## In flight

- Nothing. Every planned phase is shipped; further work starts on the owner's ask (recalibration from a recorder session, quest-aware brawler choice and opt-in auto-upgrade are on hold).

## Queue (v1)

- Empty. The legacy repo as9pa/bsutil was archived on GitHub on 2026-09-11 at the owner's request (read-only there; the local checkout stays the reference for the scrub rule).

## After v1

8. Calibration page, then recalibration for the current game version; desktop window and tray icon; labeled frame recorder.

## On hold (owner decision)

- Quest-aware brawler choice.
- Opt-in auto-upgrade.

Design: `docs/superpowers/specs/2026-09-10-brawlfarm-design.md`.
